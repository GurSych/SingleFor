#pragma once

#include <tuple>
#include <cstdint>

#include "single_for/for_generator.hpp"

namespace gtd {
    template<typename... LIMs>
    class single_for_handler;
}

template<typename... LIMs>
class gtd::single_for_handler {
public:
    
};